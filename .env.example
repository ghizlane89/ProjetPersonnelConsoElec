# Configuration Energy Agent
# Copiez ce fichier vers .env et remplissez vos valeurs

# Clé API Google Gemini (obligatoire)
GEMINI_API_KEY=votre_clé_api_gemini_ici

# Chemin vers la base de données DuckDB (optionnel, valeur par défaut)
DUCKDB_PATH=data/processed/energy_2h_aggregated.duckdb

# Niveau de log (optionnel, valeur par défaut: INFO)
LOG_LEVEL=INFO
